// The `TaskStatus` enum represents the possible states of a task.
enum TaskStatus {
  published, // Indicates the task has been made public
  pending, // Indicates the task is awaiting action or completion
  completed, // Indicates the task has been finished successfully
}
